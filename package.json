{
  "name": "speakeasy-slate",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:all": "npm run dev:server & npm run dev",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  }
}
