# `@fnl/ui`

Tokens de tema (cores, fontes) para white-label.

- Marquinho's original: tema travado (`marquinhosTheme`).
- Tenant da plataforma: injeta logo e cores por config, sem fork do CSS.

Preset Tailwind: `@fnl/ui/tailwind.preset`.
